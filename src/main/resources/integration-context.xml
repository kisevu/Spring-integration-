<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/integration
                           http://www.springframework.org/schema/integration/spring-integration.xsd">

    <int:gateway service-interface="com.ameda.kevin.integration_spring.PrinterGateway"
                 default-request-channel="inputChannel" />

    <bean id="customSplitter" class="com.ameda.kevin.integration_spring.CustomSplitter"/>
    <int:splitter ref="customSplitter" input-channel="inputChannel" output-channel="outputChannel" method="split" />

  <int:channel id="inputChannel" />
  <int:channel id="outputChannel" />


   <bean id="printService" class="com.ameda.kevin.integration_spring.PrintService" />
   <int:service-activator ref="printService" method="print" input-channel="outputChannel"/>
</beans>

<!--
* Splitter endpoint
**************************
*  Used to separate a message into multiple parts.
* Resulting parts are send separately.
* When we want to split a message in the CustomSplitter we must return one of three types;
* Collection/Array of messages
* Collection/ Array of non messages.
* A message / a non message object.
-->
