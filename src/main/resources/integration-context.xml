<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/integration
                           http://www.springframework.org/schema/integration/spring-integration.xsd">
    <int:gateway service-interface="com.ameda.kevin.integration_spring.PrinterGateway"
                 default-request-channel="inputChannel">
    </int:gateway>

    <int:channel id="inputChannel">
        <int:dispatcher failover="false" />
    </int:channel>

    <bean class="com.ameda.kevin.integration_spring.PrintService" id="printService"/>
    <bean class="com.ameda.kevin.integration_spring.UpperCasePrintService" id="upperCasePrintService"/>

    <int:service-activator order="1" ref="printService" method="print" input-channel="inputChannel">
    </int:service-activator>

    <int:service-activator  order="2" ref="upperCasePrintService" method="print" input-channel="inputChannel">
    </int:service-activator>
</beans>

<!--
 * Detail break down of the entire architecture
 ***************************************************
 * PrintService class contains the print() which receives the message
 * from the service activator which is connected to the messaging architecture
 * by DirectChannel
 * So we need demonstrating the load balancing capabilities of DirectChannel  we need creating another endpoint
 * called UpperCasePrintService which displays the wordings in uppercase in order to see our experiment
-->

