<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:int-http="http://www.springframework.org/schema/integration/http"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/integration
           https://www.springframework.org/schema/integration/spring-integration.xsd
           http://www.springframework.org/schema/integration/http
           https://www.springframework.org/schema/integration/http/spring-integration-http.xsd">


    <int-http:outbound-channel-adapter
            channel="httpOutboundChannel"
            http-method="GET"
            url="http://localhost:8888/">
    </int-http:outbound-channel-adapter>

<int-http:inbound-gateway request-channel="httpChannel" supported-methods="GET" path="/"/>

    <int:channel id="httpChannel"/>
    <int:channel id="httpOutboundChannel"/>

    <bean id="printerService" class="com.ameda.kevin.integration_spring.PrintService"/>

    <int:service-activator
            ref="printerService"
            input-channel="httpChannel"
            method="print">
    </int:service-activator>

    <int:gateway service-interface="com.ameda.kevin.integration_spring.SimpleGateway"
                 default-request-channel="httpOutboundChannel"/>

</beans>
<!--
* Http Integration support (HTTP Outbound Channel Adapter)
*******************************************************
* Allows us to send http requests from spring integration application based upon messages received
from the messaging system.
The component provides one way communication with http, Here we do not receive any replies
other than some status code like 200
* This is an outbound component and in order to interact with it, we'd require to send messages
to it and for this will be using the gateway
*********************************************
*
-->

