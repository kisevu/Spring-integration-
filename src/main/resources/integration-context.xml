<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/integration
                           http://www.springframework.org/schema/integration/spring-integration.xsd">

    <int:gateway service-interface="com.ameda.kevin.integration_spring.PrinterGateway"
                 default-request-channel="inputChannel" />
    <int:channel id="inputChannel" />
    <int:channel id="outputChannel" />

    <bean id="CustomTransformer" class="com.ameda.kevin.integration_spring.CustomTransformer"/>

    <int:transformer ref="CustomTransformer" method="transform" input-channel="inputChannel" output-channel="outputChannel"/>


   <bean id="printService" class="com.ameda.kevin.integration_spring.PrintService" />
   <int:service-activator ref="printService" method="print" input-channel="outputChannel"/>
</beans>

<!--
* Transformer endpoints
**************************
* we modify either the headers or payload when doing transformations.
* transformers allows loose coupling between endpoints because we can adjust a message types to satisfy a
consuming endpoint
* i.e. if we had an endpoint processing only Json data, however, a producer sends objects. Transformer can be
used to perform transformation from objects to json data
* Transformers are used for standardizing data and passing them downstream
*custom logic of transformation
-->
