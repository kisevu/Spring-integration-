<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/integration
                           http://www.springframework.org/schema/integration/spring-integration.xsd">

    <int:gateway service-interface="com.ameda.kevin.integration_spring.PrinterGateway"
                 default-request-channel="inputChannel" />
    <int:channel id="inputChannel" />
    <int:channel id="outputChannel" />
    <int:channel id="agChannel" />

    <bean id="customSplitter" class="com.ameda.kevin.integration_spring.CustomSplitter"/>
    <int:splitter ref="customSplitter" input-channel="inputChannel" output-channel="agChannel" method="split" />
    <int:aggregator ref="customAggregator" input-channel="agChannel" output-channel="outputChannel"/>
    <bean id="customAggregator" class="com.ameda.kevin.integration_spring.CustomAggregator"/>


   <bean id="printService" class="com.ameda.kevin.integration_spring.PrintService" />
   <int:service-activator ref="printService" method="print" input-channel="outputChannel"/>
</beans>

<!--
* Custom Aggregator endpoint
**************************
* Consolidate several messages into a message group and creates a single message from that group
* Sometimes we need a custom format of how messages are received
-->
