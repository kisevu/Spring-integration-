<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:int-http="http://www.springframework.org/schema/integration/http"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/integration
           https://www.springframework.org/schema/integration/spring-integration.xsd
           http://www.springframework.org/schema/integration/http
           https://www.springframework.org/schema/integration/http/spring-integration-http.xsd">

    <int-http:inbound-gateway
            request-channel="httpChannel"
            supported-methods="GET"
            path="/">
    </int-http:inbound-gateway>

    
    <int:channel id="httpChannel"/>

    <bean id="printerService" class="com.ameda.kevin.integration_spring.PrintService"/>
    <int:service-activator ref="printerService" input-channel="httpChannel" method="print">
    </int:service-activator>

</beans>
<!--
* Http Integration support (HTTP Inbound Gateway)
*******************************************************
* Http Inbound Gateway much similar like the http inbound.
 What it does is, it takes in the http request and pass it as a message downstream for further processing within
 spring integration messaging system. The gateway allows for two-way communication.
 A response is sent back to whatever system sent the http request to our application
*********************************************
*
-->

