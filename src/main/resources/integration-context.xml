<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:int-http="http://www.springframework.org/schema/integration/http"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/integration
           https://www.springframework.org/schema/integration/spring-integration.xsd
           http://www.springframework.org/schema/integration/http
           https://www.springframework.org/schema/integration/http/spring-integration-http.xsd">

    <int-http:inbound-channel-adapter
            path="/"
            supported-methods="GET"
            channel="httpChannel">
    </int-http:inbound-channel-adapter>
    
    <int:channel id="httpChannel"/>

    <bean id="printerService" class="com.ameda.kevin.integration_spring.PrintService"/>
    <int:service-activator ref="printerService" input-channel="httpChannel" method="printConsole">
    </int:service-activator>

</beans>
<!--
* Http Integration support (HTTP Inbound Channel Adapter)
*******************************************************
* Provides a number of Adapters and Gateways that allow our applications to integrate with REST APIS
Most of the APIs use http as the underlying protocol.
* Http Inbound channel Adapter provides one-way communication for inbound http traffic
* It allows our spring integration applications to receive http requests which are then converted to messages
that are passed downstream for further processing.
*********************************************
*
-->

