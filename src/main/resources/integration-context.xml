<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:int-http="http://www.springframework.org/schema/integration/http"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/integration
           https://www.springframework.org/schema/integration/spring-integration.xsd
           http://www.springframework.org/schema/integration/http
           https://www.springframework.org/schema/integration/http/spring-integration-http.xsd">

    <int-http:outbound-gateway
            request-channel="httpOutboundChannel"
            url="http://localhost:8888/"
            http-method="GET"
            reply-channel="httpReplyChannel">
    </int-http:outbound-gateway>

<int-http:inbound-gateway request-channel="httpChannel" supported-methods="GET" path="/"/>

    <int:channel id="httpChannel"/>
    <int:channel id="httpOutboundChannel"/>
    <int:channel id="httpReplyChannel"/>

    <bean id="printerService" class="com.ameda.kevin.integration_spring.PrintService"/>
    <bean id="printerService2" class="com.ameda.kevin.integration_spring.PrintService"/>

    <int:service-activator
            ref="printerService2"
            input-channel="httpReplyChannel"
            method="printConsole">
    </int:service-activator>

    <int:service-activator
            ref="printerService"
            input-channel="httpChannel"
            method="print">
    </int:service-activator>

    <int:gateway service-interface="com.ameda.kevin.integration_spring.SimpleGateway"
                 default-request-channel="httpOutboundChannel"/>

</beans>
<!--
* Http Integration support (HTTP Outbound Gateway)
*******************************************************
* Allows us to send http requests from our application similar to http outbound channel adapter with the difference being
that the gateway allows for the two way communication.
*********************************************
*
-->

