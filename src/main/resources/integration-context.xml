<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/integration
                           http://www.springframework.org/schema/integration/spring-integration.xsd">

    <int:gateway service-interface="com.ameda.kevin.integration_spring.PrinterGateway"
                 default-request-channel="inputChannel" />

  <int:channel id="inputChannel" />
  <int:channel id="outputChannel" />

  <int:filter expression="payload.equals(4)" input-channel="inputChannel" output-channel="outputChannel" />

   <bean id="printService" class="com.ameda.kevin.integration_spring.PrintService" />
   <int:service-activator ref="printService" method="print" input-channel="outputChannel"/>
</beans>

<!--
* Filters
**************************
* Filters are endpoints that decide whether a message should be passed or should be dropped.
* In spring security we have a use of the filter
* The underlying logic of the filters can be configured in several ways
* expression attribute is consulted whether the message should be dropped or proceed to the output
-->
